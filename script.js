let num = 0
let memtrue = 0
let level = 1;
function upgrade() {
  if (num >= 10) {
    num -= 10;
    level++;
    document.title = `Ouro: ${num}`;
}}
function Gaingold() {
  num+
  document.title == `Ouro: ${num}`;
  const titulo = document.querySelector("h1");
  titulo.textContent =  `${num}`;
}
